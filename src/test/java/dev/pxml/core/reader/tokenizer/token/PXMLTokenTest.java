package dev.pxml.core.reader.tokenizer.token;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.DisplayName;

import java.io.File;

// TODO remove

public class PXMLTokenTest {

/*
    @Test
    @DisplayName ( "toString() should work" )
    public void testToString() {

        PXMLToken token = new PXMLToken ( "value", 10, 12, null, null );
        assertEquals ( token.toString(), "\"value\" at (10, 12)" );

        token = new PXMLToken ( "value 2", 123, 12, new File( "tests/foo.txt" ), null );
        assertEquals ( token.toString(), "\"value 2\" at (123, 12) in tests\\foo.txt" );
    }
*/
}
